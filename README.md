# Aprendizajes del modulo fundamentos del desarrollo web
En este primer proyecto se reutilizo la pagina web que se desarrollo en el transcurso del modulo 1 teniendo el `Index` y hoja de estilos (`css`) anterior con estructura basica de diseño. 


En este proyecto se implemento:
* Etiqueta ` <button>`
* el efecto **Hover** (en el boton con un color azul al pasar el raton)
* Uso de selectores de tipo `body`, `img`, `button` y de clase como `.start`.
*  pseudo-clases como `button:hover`.
* Uso de `Display:block` en `img`

